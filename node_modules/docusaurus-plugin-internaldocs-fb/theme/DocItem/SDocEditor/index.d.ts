/**
 * (c) Meta Platforms, Inc. and affiliates. Confidential and proprietary.
 *
 * @format
 */
/// <reference types="react" />
import { DiffKind } from '../../../internal/inpageeditor';
export declare type EditorProps = {
    handleCloseEditor: () => void;
    isSubmitting: boolean;
    onEditorSubmit: () => void;
    pageRawContent: string | null;
    diffKind: DiffKind;
    newFilePath: string;
    setPageRawContentVersion: (value: {
        pageRawContent: string;
        timestamp: string;
        diffKind: DiffKind;
        newFilePath: string;
    }) => void;
};
export declare function SDocEditor({ onEditorSubmit, handleCloseEditor, pageRawContent, setPageRawContentVersion, isSubmitting, diffKind, newFilePath, }: EditorProps): JSX.Element | null;
